extends layout/login-layout.pug

block content
    div.loginContainer
        h1 Login
        form(method="post")
            p.errorMessage #{errorMessage}
            input(type="text", name="logUsername", placeholder="Username or email", value="dungnx" required="")
            input(type="password", name="logPassword", placeholder="Password", value="123" required="")
            input(type="submit", value="Login")
        a(href="/register") Need an account? Register here.